services:
  # A Docker web service
  - type: web
    name: clockmayhem-fastapi-example
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn gameserver:app --host 0.0.0.0 --port $PORT   startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
